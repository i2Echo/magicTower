
//地图图片数组
var map;
//地图地形数组
var mapdata ;

//function aRoadTile(x, y){
//  var bitmapdata,bitmap;
//  bitmapdata = new LBitmapData(imgList["map"],2*STEP,0*STEP,STEP,STEP);
//  bitmap = new LBitmap(bitmapdata);
//  bitmap.x = x*STEP;
//  bitmap.y = y*STEP;
//
//  return bitmap
//}